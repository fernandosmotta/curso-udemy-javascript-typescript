let nome = 'Laila'
let idade = 9

const situacao = ( idade >= 10 ) ? `${nome} é uma cachorra idosa` : `${nome} é novinha ainda`;

console.log(situacao);


const corUsuario = `Blue`;

const colorSystem = (corUsuario) ? corUsuario : 'Black';

console.log( colorSystem );